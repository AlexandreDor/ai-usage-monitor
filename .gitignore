# Secrets and local state — never commit these
local/.env
local/data.json
local/history.json
local/.alert_state

# Node / npm (if ever used locally)
node_modules/
npm-debug.log*

# macOS
.DS_Store

# Windows
Thumbs.db
desktop.ini
