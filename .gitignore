# Secrets and local state — never commit these
local/.env
local/.alert_state
local/.monitor.lock
local/data.json
local/history.json
local/runtime/

# User alert scripts stay local. Keep only the directory placeholder tracked.
local/scripts/*
!local/scripts/.gitkeep
local/tests/

# Node / npm (if ever used locally)
node_modules/
npm-debug.log*
__pycache__/

# macOS
.DS_Store

# Windows
Thumbs.db
desktop.ini

# Local/internal-only repo content
.claude/
WHY.MD
agent.md
Dockerfile
docker-compose.yml
docker-entrypoint.sh
openclaw-webinfo/
